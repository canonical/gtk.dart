name: gtk_settings_example
publish_to: 'none'

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  gtk_settings:
    path: ../
  provider: ^6.0.5
  yaru: ^0.4.7

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
